export class Task {
    constructor(title, description, dueDate, project, priority, checkStatus) {
        this.title = title
        this.description = description
        this.dueDate = dueDate
        this.project = project
        this.priority = priority
        this.checkStatus = checkStatus
    }
}